# OmniKey Vault — 中文使用手册

**版本**: 1.7.0
**更新日期**: 2026-07-15
**适用平台**: Windows 10/11 (x64)

---

## 目录

1. [产品简介](#1-产品简介)
2. [安装与卸载](#2-安装与卸载)
3. [创建保险库](#3-创建保险库)
4. [解锁与锁定](#4-解锁与锁定)
5. [条目管理](#5-条目管理)
6. [配置文件 (Profile)](#6-配置文件-profile)
7. [WebDAV 云同步](#7-webdav-云同步)
8. [本地文件夹同步](#8-本地文件夹同步)
9. [搜索与筛选](#9-搜索与筛选)
10. [TOTP 双因素验证码](#10-totp-双因素验证码)
11. [剪贴板管理](#11-剪贴板管理)
12. [自动锁定与安全设置](#12-自动锁定与安全设置)
13. [导入与导出](#13-导入与导出)
14. [历史快照](#14-历史快照)
15. [安全设计](#15-安全设计)
16. [常见问题](#16-常见问题)
17. [键盘快捷键](#17-键盘快捷键)

---

## 1. 产品简介

OmniKey Vault 是一款**本地优先、端到端加密**的密码与密钥管理器。所有密码学操作在您的设备本地完成，主密码永不出设备。

### 核心特性

- **双重 Argon2id 密钥拉伸（v1.6 新增）**: 双轮 KDF 链（总计 320 MiB 内存成本），即使源码公开+金库文件泄露，离线爆破成本也翻倍
- **零知识加密**: 使用 Argon2id 密钥派生 + XChaCha20-Poly1305 AEAD 加密 + Ed25519 签名
- **自定义格式**: `.okv` 二进制格式，不被 hashcat / KeePass 等通用工具识别
- **多设备同步**: 支持 WebDAV 云同步与本地文件夹同步，使用向量时钟解决冲突
- **多 Profile**: 生产 (prod)、开发 (dev)、测试 (test) 环境隔离
- **TOTP 支持**: 内置 OTP 验证码生成器
- **平台模板**: 内置 50 个模板，覆盖国内外主流平台（GitHub、OpenAI、AWS、腾讯云、华为云、百度千帆、通义千问、DeepSeek、支付宝、微信支付等）
- **自动锁定**: 空闲超时、系统锁屏、系统休眠自动锁定
- **剪贴板保护**: 复制后自动清空剪贴板
- **开源安全**: 源代码完全公开，接受社区审计，安全设计不依赖隐蔽

---

## 2. 安装与卸载

### 2.1 系统要求

- Windows 10 1809 (17763) 或更高版本
- .NET 8 运行时（安装包已内置）

### 2.2 安装

1. 双击 `OmniKeyVault-Setup.exe` 运行安装程序
2. 选择安装目录（默认: `C:\Program Files\OmniKeyVault`）
3. 选择是否创建桌面快捷方式
4. 点击「安装」等待完成
5. 点击「完成」退出安装程序

### 2.3 卸载

- **方式一**: 打开「设置 → 应用 → 已安装的应用」，找到 OmniKey Vault，点击卸载
- **方式二**: 运行安装目录下的 `uninstall.exe`

> **注意**: 卸载不会删除您的金库文件 (`vault.okv`) 和配置文件。金库文件位于 `%USERPROFILE%\OmniKeyVault\`，配置文件位于 `%APPDATA%\OmniKeyVault\`。

### 2.4 首次启动

首次启动时会显示解锁窗口。如果没有现有金库，点击「创建新金库」按钮。

---

## 3. 创建保险库

### 3.1 创建步骤

1. 在解锁窗口点击「创建新金库」
2. **步骤 1 — 选择路径**: 选择金库文件的保存位置（默认: `%USERPROFILE%\OmniKeyVault\vault.okv`）
3. **步骤 2 — 设置主密码**:
   - 输入主密码（至少 8 个字符）
   - 确认主密码
   - 密码强度指示器会实时显示密码强度
   - **弱密码将被拒绝**（评分 < 2 的密码无法创建金库）
   - 建议使用 12+ 字符，包含大小写字母、数字和符号
4. **步骤 3 — 确认并保存**: 勾选「我已安全保存主密码」，点击「创建」
5. **步骤 4 — 恢复密钥**: 创建完成后会显示 192 字符的恢复密钥，请妥善保存

### 3.2 从云端拉取已有金库（v1.5 新增）

如果您已在其他设备上将金库同步到 WebDAV 云端，在新设备上无需创建新金库，可直接从云端拉取：

1. 在创建金库向导的欢迎页面，点击「☁ 从云端拉取已有金库 →」
2. 填写 WebDAV 配置：
   - **服务器地址**: 如 `https://dav.jianguoyun.com/dav/omnikey-vault/`
   - **用户名**: WebDAV 账号
   - **密码**: WebDAV 应用密码
   - **远端文件路径**: 如 `vault.okv`
   - **本地保存文件夹**: 选择本地保存位置
3. （可选）点击「🔗 测试连接」验证配置是否正确
4. 点击「拉取金库 →」
5. 拉取成功后，程序自动跳转到解锁窗口
6. 输入与原设备相同的主密码即可解锁

> **优势**: 跳过新建金库的完整流程（设置密码、生成恢复密钥等），直接使用云端已有金库登录。
>
> **注意**: 坚果云用户需在服务器地址中包含子目录路径（如 `/dav/omnikey-vault/`），不能直接使用根路径 `/dav/`。

### 3.3 主密码安全建议

| 强度 | 示例 | 建议 |
|------|------|------|
| 弱 | `password123` | 不要使用 |
| 中 | `MyCat2024!` | 仅用于测试 |
| 强 | `Tr0ub4dour&3#sky` | 可接受 |
| 很强 | `correct-horse-battery-staple-2026` | 推荐 |

> **警告**: 主密码是唯一保护您金库的密码。如果遗忘，**无法恢复**（即使恢复密钥也需要主密码来使用）。

### 3.4 恢复密钥

恢复密钥是一串 192 字符的随机字符串，在创建金库时生成。它的作用是：

- 在主密码遗忘时，可作为应急验证手段
- 请离线保存（如打印后锁在保险箱中）
- **切勿与金库文件保存在同一位置**

---

## 4. 解锁与锁定

### 4.1 解锁

1. 启动程序后显示解锁窗口
2. 确认金库路径（程序会自动记住上次打开的金库）
3. 输入主密码
4. 点击「解锁」或按 Enter 键
5. 解锁过程需要约 2-4 秒（v1.6 双重 Argon2id 密钥派生，总内存成本 320 MiB；v1 金库约 1-3 秒）

### 4.2 锁定

以下方式可以锁定金库：

- 点击顶部状态栏的「锁定」按钮
- 按 `Ctrl+L` 快捷键
- 空闲超时自动锁定
- 系统锁屏时自动锁定（可配置）
- 系统休眠时自动锁定（可配置）

锁定后，内存中的所有密钥和明文数据会被**立即清零** (`CryptographicOperations.ZeroMemory`)。

### 4.3 自动锁定

默认 15 分钟无操作后自动锁定。可在「设置 → 安全」中修改为 5/10/15/30/60 分钟。

---

## 5. 条目管理

### 5.1 新建条目

1. 点击左侧边栏的「+ 新建条目」按钮
2. 在编辑器窗口中填写：
   - **名称**: 条目名称（如 "OpenAI 生产环境"）
   - **平台**: 选择平台模板（GitHub / OpenAI / AWS / Stripe 等）
   - **字段**: 每个条目可包含多个字段，类型包括：
     - `密文` — 敏感信息（API Key、密码等），默认掩码显示
     - `文本` — 普通文本
     - `链接` — URL
     - `数字` — 数字
     - `日期` — 日期
     - `TOTP` — OTP 验证码 URI
     - `文件` — 文件引用
   - **标签**: 用空格分隔的标签（如 `prod critical`）
   - **备注**: 自由文本
   - **文件夹**: 可选，将条目归入文件夹
3. 点击「保存」

### 5.2 编辑条目

- 点击条目列表中的条目，然后点击「编辑」按钮
- 或右键点击条目，选择「编辑」

### 5.3 删除条目

- 右键点击条目，选择「删除」
- 确认后删除（不可撤销）

### 5.4 复制字段

- 点击条目详情中字段右侧的「⧉」按钮复制
- 或右键点击条目，选择「复制主字段」
- 复制后 8 秒自动清空剪贴板（可配置为 4/8/16/30 秒）

### 5.5 显示明文

对于 `密文` 类型的字段，点击「👁」按钮按住可临时显示明文，松开后自动掩码。

### 5.6 排序

点击顶部「排序」按钮可选择：
- 按名称 (A→Z)
- 按更新时间 (新→旧)
- 按创建时间 (新→旧)

---

## 6. 配置文件 (Profile)

OmniKey Vault 支持多配置文件 (Profile) 来隔离不同环境的密钥：

| Profile | 颜色 | 用途 | 横幅 |
|---------|------|------|------|
| `prod` | 绿色 | 生产环境 | 无 |
| `dev` | 黄色 | 开发环境 | "DEV — 非生产数据" |
| `test` | 蓝色 | 测试环境 | "TEST — 测试数据" |

### 6.1 切换 Profile

点击顶部状态栏的 Profile 标签，在弹出窗口中选择目标 Profile。

### 6.2 创建新 Profile

在设置窗口的「Profiles」部分，或通过种子导入功能创建新 Profile。

### 6.3 Profile 设置

每个 Profile 可独立配置：
- **参与同步**: 是否参与多设备同步
- **切换后自动锁定**: 切换 Profile 后立即锁定金库
- **空闲锁**: 空闲锁定时间

---

## 7. WebDAV 云同步

WebDAV 同步是 OmniKey Vault 的核心云同步功能，允许您在多台设备之间同步金库。

### 7.1 工作原理

```
设备 A (本地金库)                    WebDAV 服务器                  设备 B (本地金库)
     │                                   │                              │
     │── 1. 下载远端 .okv ──────────────→│                              │
     │←── 2. 返回远端 .okv ──────────────│                              │
     │                                   │                              │
     │   3. 向量时钟比较 + 条目合并       │                              │
     │   4. 保存合并后的本地 .okv         │                              │
     │                                   │                              │
     │── 5. 上传合并后的 .okv ──────────→│                              │
     │                                   │                              │
     │                                   │←── 6. 设备 B 下载 .okv ──────│
     │                                   │── 7. 返回 .okv ──────────────→│
     │                                   │                              │
     │                                   │←── 8. 设备 B 合并 + 上传 ────│
```

**关键安全保证**: WebDAV 服务器只存储**加密后的 `.okv` 文件**。即使服务器被入侵，攻击者也无法解密您的数据。

### 7.2 支持的 WebDAV 服务

以下服务均已测试兼容：

| 服务 | 服务器地址格式 | 备注 |
|------|---------------|------|
| Nextcloud / ownCloud | `https://your-server/remote.php/dav/files/username/okv/` | 需要应用密码 |
| Synology NAS | `https://your-nas:5006/okv/` | 需要 WebDAV Server 套件 |
| 坚果云 | `https://dav.jianguoyun.com/dav/okv/` | 需要应用密码（非登录密码） |
| Box | `https://dav.box.com/dav/okv/` | 需要应用密码 |
| 自建 WebDAV | `https://your-server/path/` | 需支持 RFC 4918 |

### 7.3 配置 WebDAV

1. 打开主界面，点击「⚙ 设置」
2. 找到「WebDAV 云同步」部分
3. 填写以下信息：
   - **服务器地址**: WebDAV 服务器的完整 URL
     - 例如: `https://dav.example.com/okv/`
     - URL 末尾建议加 `/`，远端文件路径会拼接到此 URL 后
   - **用户名**: WebDAV 账号用户名
   - **密码**: WebDAV 应用密码（**不是**您的登录密码）
     - 大多数 WebDAV 服务要求使用专用的应用密码
     - 坚果云: 在网页端「安全选项 → 添加应用」生成
     - Nextcloud: 在「设置 → 安全 → 设备和会话」生成
   - **远端文件路径**: 金库在服务器上的文件名
     - 默认: `vault.okv`
     - 如果多个金库使用同一服务器，可用不同文件名区分
4. 勾选「启用 WebDAV 同步」
5. 点击「🔗 测试连接」验证配置
   - 成功: 显示 "✓ 连接成功！服务器可访问。"
   - 失败: 显示具体错误信息
6. 点击「💾 保存配置」持久化设置

### 7.4 手动同步

配置完成后，在主界面顶部有三个同步按钮，分别对应不同的操作：

| 按钮 | 功能 | 使用场景 |
|------|------|----------|
| ⬇ 拉取云端 | 从 WebDAV 服务器下载并合并远端更改 | 在其他设备上编辑后，想在当前设备获取更新 |
| ⬆ 推送到云端 | 直接上传本地金库到 WebDAV 服务器 | 本地有更新，想同步到其他设备 |
| 🔄 本地同步 | 重新加载本地金库文件 | 从本地备份恢复或手动替换文件后刷新 |

**推荐使用方式**：
- **多设备协作**: 优先使用「⬇ 拉取云端」获取远端更新，编辑后使用「⬆ 推送到云端」同步
- **单设备备份**: 使用「⬆ 推送到云端」定期上传备份
- **本地恢复**: 使用「🔄 本地同步」加载手动复制的金库文件

同步结果会通过 Toast 提示：
- **无变化**: 远端和本地一致
- **拉取**: 从远端拉取了更新
- **推送**: 本地版本更新，已推送到远端
- **合并**: 双方都有更新，已自动合并
- **首次上传**: 远端不存在文件，已上传本地金库

### 7.5 自动同步

勾选「自动同步（解锁后拉取，保存后推送）」后：
- 每次解锁金库后，自动从 WebDAV 拉取最新数据
- 每次保存条目后，自动推送更新到 WebDAV

> **建议**: 在多设备使用场景下开启自动同步。在网络不稳定时，自动同步失败不会影响本地操作。

### 7.6 冲突解决

当两台设备同时修改了金库，同步时会检测到冲突。OmniKey Vault 使用以下策略：

1. **向量时钟比较**:
   - 本地领先 → 推送本地版本到远端
   - 远端领先 → 拉取远端版本到本地
   - 并发修改 → 执行条目级合并

2. **条目级合并规则**:
   - 新条目（仅一方有）→ 直接添加
   - 版本不同的条目 → 保留较高版本
   - 同版本不同内容 → **本地优先** (local-wins)，并提示冲突数量

3. **冲突提示**: 同步完成后会显示冲突数量。如需覆盖为远端版本，可使用本地文件夹同步的冲突解决向导。

### 7.7 首次使用 WebDAV 同步

**设备 A（已有金库）**:
1. 打开设置，配置 WebDAV
2. 点击「同步」按钮 → 首次上传金库到服务器

**设备 B（新设备）**:

**方式一：从云端拉取（v1.5 推荐）**:
1. 安装 OmniKey Vault
2. 在解锁窗口点击「创建新金库」
3. 在欢迎页面点击「☁ 从云端拉取已有金库 →」
4. 填写与设备 A 相同的 WebDAV 配置
5. 选择本地保存文件夹，点击「拉取金库」
6. 拉取成功后，输入与设备 A 相同的主密码解锁即可

**方式二：传统创建+同步**:
1. 安装 OmniKey Vault
2. 在解锁窗口点击「创建新金库」，**使用与设备 A 相同的主密码**
3. 打开设置，配置相同的 WebDAV 信息
4. 点击「同步」按钮 → 从服务器拉取金库
5. 程序会提示「请锁定后使用远端金库的密码重新解锁」
6. 锁定金库后重新解锁，即可看到设备 A 的所有条目

> **重要**: 两台设备必须使用**相同的主密码**创建金库，因为 KEK（密钥加密密钥）是从主密码派生的。如果主密码不同，远端文件的加密密钥不同，无法解密。
>
> **v1.2 新增**: 如果新设备创建的是空金库（无任何条目），同步时会自动拉取远端金库替换本地。如果本地金库已有数据且与远端不匹配，会提示「金库不匹配」错误，避免数据丢失。
>
> **v1.5 新增**: 在创建金库向导中直接从云端拉取已有金库，跳过新建流程，更简单快捷。

### 7.8 WebDAV 安全注意事项

1. **务必使用 HTTPS**: 确保服务器地址以 `https://` 开头，防止中间人攻击
2. **使用应用密码**: 不要使用登录密码，大多数服务支持生成专用的应用密码
3. **服务器只存密文**: 即使服务器管理员也无法看到您的明文数据
4. **定期检查设备列表**: 在设置中查看已注册设备，及时吊销不认识的设备
5. **备份金库文件**: 建议在本地保留金库文件的备份副本

---

## 8. 本地文件夹同步

除了 WebDAV，OmniKey Vault 还支持本地文件夹同步，适用于：
- 网络共享文件夹
- 云盘本地同步目录（OneDrive、Google Drive、Dropbox 等）
- USB 移动存储

### 8.1 配置

1. 打开「设置 → 同步」
2. 在「同步目录」中填写或浏览选择同步文件夹
3. 勾选「启用文件监听」以自动检测远端变化

### 8.2 使用

点击主界面的「同步」按钮（在未配置 WebDAV 时），会弹出文件夹选择器：
1. 选择包含另一个 `.okv` 文件的目录
2. 程序会自动找到目录中的 `.okv` 文件
3. 执行向量时钟比较和合并

### 8.3 文件监听

启用文件监听后，当同步目录中的 `.okv` 文件发生变化时（如其他设备更新了云盘文件），程序会显示 Toast 提示「检测到远端更新，点击同步查看最新」。

---

## 9. 搜索与筛选

### 9.1 快速搜索

在主界面顶部的搜索框中输入关键词，250ms 防抖后自动过滤。搜索范围包括：
- 条目名称
- 字段键名和值
- 标签
- 备注

### 9.2 高级搜索

点击搜索框旁的「🔍」按钮打开高级搜索窗口，支持：
- 字段级语法: `name:github secret:token`
- 正则匹配
- 平台筛选
- 标签筛选

### 9.3 文件夹筛选

左侧边栏的文件夹面板支持：
- 「全部条目」— 显示所有条目
- 「未分类」— 仅显示未归入文件夹的条目
- 点击具体文件夹 — 按文件夹筛选

### 9.4 标签筛选

点击左侧边栏底部的标签按钮，快速搜索该标签下的所有条目。

---

## 10. TOTP 双因素验证码

OmniKey Vault 内置 TOTP（基于时间的一次性密码）生成器。

### 10.1 添加 TOTP

1. 在条目编辑器中添加一个 `TOTP` 类型的字段
2. 填入 `otpauth://` 格式的 URI，例如：
   ```
   otpauth://totp/OpenAI:user@example.com?secret=JBSWY3DPEHPK3PXP&issuer=OpenAI&digits=6&period=30
   ```

### 10.2 查看验证码

在条目详情中，TOTP 字段会显示：
- 6 位或 8 位验证码（每 30 秒刷新）
- 剩余秒数和倒计时进度环
- 支持一键复制

### 10.3 获取 OTP URI

- 从 Google Authenticator / Microsoft Authenticator 导出
- 从服务提供商的 2FA 设置页面获取
- 手动拼接：`otpauth://totp/Label?secret=BASE32SECRET&digits=6&period=30`

---

## 11. 剪贴板管理

### 11.1 复制与自动清空

- 点击字段旁的「⧉」按钮复制值到剪贴板
- 复制后默认 8 秒自动清空剪贴板
- 清空时间可在「设置 → 安全 → 剪贴板自动清空」中修改（4/8/16/30 秒）
- 清空后显示 Toast 提示「剪贴板已清空」

### 11.2 安全说明

- 复制的值在剪贴板中的存活时间有限
- 不要在复制后立即粘贴到不信任的应用中
- 如果需要长时间保留，建议使用密码管理器内的查看功能而非复制

---

## 12. 自动锁定与安全设置

### 12.1 自动锁定

| 触发条件 | 默认 | 可配置 |
|----------|------|--------|
| 空闲超时 | 15 分钟 | 5/10/15/30/60 分钟 |
| 系统锁屏 | 立即锁定 | 可关闭 |
| 系统休眠 | 立即锁定 | 可关闭 |

### 12.2 主题切换

在「设置 → 通用 → 主题」中可选择：
- 跟随系统
- 深色
- 浅色

### 12.3 语言

支持简体中文 (zh-CN) 和英文 (en-US)。

### 12.4 修改主密码

1. 打开「设置 → 危险操作 → 修改主密码」
2. 输入当前主密码
3. 输入新主密码（≥ 8 字符）并确认
4. 修改后所有备份和同步远端会失效，需要重新同步

> **警告**: 修改主密码后，其他设备的旧 `.okv` 文件将无法使用新密码解锁。请确保所有设备都同步更新。

### 12.5 查看恢复密钥

在「设置 → 危险操作 → 查看恢复密钥」可重新查看创建时生成的恢复密钥。

---

## 13. 导入与导出

### 13.1 从 Bitwarden 导入

1. 在 Bitwarden 中导出 JSON 格式的数据
2. 在 OmniKey Vault 主界面点击「导入」
3. 选择 Bitwarden JSON 文件
4. 条目会导入到当前 Profile

### 13.2 从 KeePass 导入

1. 在 KeePass 中「文件 → 导出 → KeePass 2 XML」
2. 在 OmniKey Vault 中使用「导入 KeePass」功能
3. 选择 XML 文件

### 13.3 导出种子文件

1. 点击「导出」
2. 选择导出路径
3. 生成 `.dev` 格式的种子文件
4. 种子文件包含加密后的条目数据，可用于在其他 Profile 导入

### 13.4 种子导入

1. 点击「导入种子」
2. 选择 `.dev` 文件
3. 条目导入到当前 Profile

---

## 14. 历史快照

每次编辑条目并保存时，OmniKey Vault 会自动创建历史快照。

### 14.1 查看历史

右键点击条目，选择「🕐 查看历史」打开历史窗口。可以：
- 查看每次修改的时间、设备、版本号
- 对比不同版本的字段差异
- 恢复到指定历史版本

### 14.2 快照存储

历史快照存储在 `%APPDATA%\OmniKeyVault\.okv.snapshots\` 目录下，采用与金库相同的加密标准。

---

## 15. 安全设计

### 15.1 加密架构

#### v1.6 金库（Header v2 — 双重 Argon2id 密钥拉伸）

```
主密码 (不存储)
    │
    ▼
Argon2id Round 1 (t=3, m=256MiB, p=4)  ← 第一轮：全量内存成本
    │
    ▼
MK1 (中间主密钥)
    │
    ▼
Argon2id Round 2 (t=3, m=64MiB, p=1)   ← 第二轮：二次拉伸
    │
    ▼
MK2 (最终主密钥)
    │
    ▼
HKDF-SHA256(MK2, "okv-kek-v2", salt1)   ← 域分隔符升级
    │
    ▼
KEK (密钥加密密钥)
    │
    ▼
解包 DEK (数据加密密钥)
    │
    ▼
XChaCha20-Poly1305 AEAD 加密
    │
    ▼
加密后的 Profile 负载
    │
    ▼
Ed25519 签名 → 写入 .okv 文件
```

#### v1 金库（Header v1 — 单轮 Argon2id，向后兼容）

```
主密码 (不存储)
    │
    ▼
Argon2id KDF (t=3, m=256MiB, p=4)
    │
    ▼
HKDF-SHA256(MK, "okv-kek-v1", salt)
    │
    ▼
KEK → DEK → XChaCha20-Poly1305 → Ed25519 签名
```

> **v1.6 安全升级**: 新创建的金库默认使用 Header v2（双重 Argon2id）。已有的 v1 金库仍可正常解锁和使用，系统会自动识别版本并选择对应的 KDF 路径。攻击者每次密码猜测需运行两轮 Argon2id（共 320 MiB 内存），爆破成本相比 v1 翻倍。

### 15.2 .okv 文件安全性

- **加密**: XChaCha20-Poly1305 AEAD 认证加密
- **KDF**: 双重 Argon2id（v1.6 Header v2: Round 1 256 MiB + Round 2 64 MiB = 总计 320 MiB；v1 Header v1: 单轮 256 MiB）
- **签名**: Ed25519 容器签名，防篡改
- **格式**: 自定义二进制格式，不被 hashcat / KeePass / John 等工具识别
- **域分隔**: KEK 派生标识从 `okv-kek-v1` 升级为 `okv-kek-v2`，确保 v1/v2 密钥不可互换

### 15.3 威胁防御

| 威胁 | 防御措施 |
|------|----------|
| 云盘被入侵 | 零知识加密，服务器只见密文 |
| 离线爆破 | 双重 Argon2id（320 MiB 内存成本）+ 自定义格式 |
| 源码+金库同时泄露 | v1.6 双轮 KDF 使爆破成本翻倍，域分隔符确保密钥不可互换 |
| 内存 dump | 敏感数据用完即清零 |
| 篡改文件 | AEAD 认证 + Ed25519 签名 |
| 重放攻击 | 向量时钟比较 |
| 未知设备 | 设备信任机制 + 公钥验证 |

### 15.4 内存安全

- 所有敏感数据（主密码、KEK、DEK）使用 `ReadOnlySpan<byte>` / `Memory<byte>`
- 锁定时调用 `CryptographicOperations.ZeroMemory` 清零
- 禁止业务层直接调用底层加密库

### 15.5 日志脱敏

所有日志输出经过 `LogRedactor` 过滤，自动脱敏：
- API Key 和密钥
- 密码和令牌
- 邮箱地址
- IP 地址

---

## 16. 常见问题

### Q: 忘记主密码怎么办？

A: 主密码无法恢复。如果您保存了恢复密钥，可以在解锁窗口尝试使用恢复密钥验证（仍需知道主密码）。如果完全没有备份，金库中的数据将无法恢复。

### Q: WebDAV 同步失败怎么办？

A: 请按以下步骤排查：
1. 检查服务器地址是否正确（以 `https://` 开头）
2. 确认使用的是**应用密码**而非登录密码
3. 点击「测试连接」验证连通性
4. 检查远端文件路径是否正确
5. 确认网络连接正常

### Q: 两台设备使用不同主密码能同步吗？

A: 不能。两台设备必须使用**相同的主密码**创建金库，因为 KEK 是从主密码派生的。如果主密码不同，远端文件的加密密钥不同，无法解密。

### Q: 同步时出现冲突怎么办？

A: 冲突会自动按「本地优先」原则解决。如需采用远端版本：
1. 使用本地文件夹同步
2. 在冲突解决向导中选择「采用远端版本」
3. 本地文件会被备份为 `.pre-sync-*` 文件

### Q: .okv 文件可以直接放到云盘吗？

A: 可以。`.okv` 文件是端到端加密的，即使云盘服务商也无法解密。您可以使用 WebDAV 同步或直接将文件放在云盘同步目录中。

### Q: 如何在多台电脑使用同一个金库？

A: 推荐使用 WebDAV 同步：
1. 在第一台电脑创建金库并配置 WebDAV
2. 在其他电脑用相同主密码创建金库
3. 配置相同的 WebDAV 信息
4. 点击同步即可拉取数据

### Q: 卸载后金库数据会丢失吗？

A: 不会。卸载只删除程序文件。金库文件 (`vault.okv`) 保存在 `%USERPROFILE%\OmniKeyVault\`，配置文件保存在 `%APPDATA%\OmniKeyVault\`。

### Q: 支持哪些平台模板？

A: 内置 50 个平台模板，覆盖国内外主流平台：

**国际平台（11 个）**：GitHub、OpenAI、AWS IAM、AWS STS、Azure、GCP、Stripe、Supabase、Anthropic、Slack、阿里云 RAM

**国内云厂商（10 个）**：腾讯云 CAM、华为云、百度智能云、京东云、火山引擎、七牛云、又拍云、UCloud、腾讯云 COS、阿里云 OSS

**国内 AI / LLM（10 个）**：百度千帆（ERNIE）、阿里通义千问（DashScope）、智谱 AI（GLM）、月之暗面（Kimi）、火山方舟（豆包）、DeepSeek、百川智能、MiniMax、零一万物（Yi）、阶跃星辰（Step）

**国内代码托管（3 个）**：Gitee（码云）、CODING（腾讯）、GitCode（CSDN）

**国内支付（3 个）**：支付宝、微信支付、银联支付

**国内通讯/推送（10 个）**：钉钉、飞书、企业微信、极光推送、个推、环信、融云、腾讯云短信、小米推送、华为推送（HMS）

**国内 SaaS（3 个）**：微信开放平台、微信公众平台（公众号/小程序）、哔哩哔哩

还可以通过 `templates/` 目录添加自定义模板。

---

## 17. 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 解锁金库（在解锁窗口） |
| `Ctrl+L` | 锁定金库 |
| `Ctrl+N` | 新建条目 |
| `Ctrl+F` | 聚焦搜索框 |
| `Ctrl+R` | 手动同步 |
| `Esc` | 关闭对话框 |
| `Delete` | 删除选中条目（需确认） |

---

## 附录

### A. 文件位置

| 文件 | 路径 | 说明 |
|------|------|------|
| 金库文件 | `%USERPROFILE%\OmniKeyVault\vault.okv` | 加密金库 |
| 配置文件 | `%APPDATA%\OmniKeyVault\settings.json` | 应用设置 |
| 上次金库 | `%APPDATA%\OmniKeyVault\last-vault.txt` | 上次打开的金库路径 |
| 历史快照 | `%APPDATA%\OmniKeyVault\.okv.snapshots\` | 历史版本备份 |
| 附件 | `%APPDATA%\OmniKeyVault\attachments\` | 加密附件存储 |
| 模板 | 程序目录 `templates\` | 平台模板 |
| 清单文件 | 金库同目录 `manifest.json` | 同步元数据 |

### B. 加密参数

#### v1.6 金库（Header v2）

| 参数 | Round 1 | Round 2 |
|------|---------|---------|
| KDF 算法 | Argon2id | Argon2id |
| KDF 时间成本 (t) | 3 | 3 |
| KDF 内存成本 (m) | 256 MiB | 64 MiB |
| KDF 并行度 (p) | 4 | 1 |
| Salt 长度 | 16 bytes (salt 槽前 16B) | 16 bytes (salt 槽后 16B) |
| KEK 派生 | HKDF-SHA256(MK2, "okv-kek-v2", salt1) | — |
| **总内存成本** | **320 MiB** | |

#### v1 金库（Header v1，向后兼容）

| 参数 | 值 |
|------|-----|
| KDF 算法 | Argon2id |
| KDF 时间成本 (t) | 3 |
| KDF 内存成本 (m) | 256 MiB |
| KDF 并行度 (p) | 4 |
| KEK 派生 | HKDF-SHA256(MK, "okv-kek-v1", salt) |

#### 通用参数

| 参数 | 值 |
|------|-----|
| 对称加密 | XChaCha20-Poly1305 |
| 签名算法 | Ed25519 |
| Nonce 长度 | 24 bytes (XChaCha20) |
| Tag 长度 | 16 bytes (Poly1305) |

### C. 联系与反馈

- 问题反馈: GitHub Issues
- 文档: docs/项目手册.md
- 安全报告: docs/安全设计.md

---

*OmniKey Vault — 本地优先，安全至上。*
