# OmniKey Vault — 测试报告

| 文档版本 | 日期 | 作者 | 状态 |
|---|---|---|---|
| 2.3 | 2026-07-23 | Sisyphus | 通过 (791/791 tests) — v2.3.8 发布 |

> **测试基线**：.NET 8.0 on Windows x64, libsodium 1.0.18, Sodium.Core 1.3.2, 0 skipped, 0 flaky。

---

## 1. 执行摘要

| 指标 | 值 |
|---|---|
| **总测试数** | 791 |
| **通过** | 791 (100%) |
| **失败** | 0 |
| **跳过** | 0 |
| **测试耗时** | ~30s |
| **界面功能验证** | 142/142 项通过 (100%) |

### 测试项目分布

| 项目 | 测试数 | 说明 |
|---|---|---|
| `OmniKeyVault.Tests` | 771 | 单元 / 集成 / CLI / GUI flow 测试 |
| `OmniKeyVault.Analyzers.Tests` | 20 | Roslyn 安全分析器测试（OKV0001 + OKV0003） |
| **合计** | **791** | |

### 测试覆盖范围

- **密码学**：Argon2id KDF（单轮 + 双轮）、XChaCha20-Poly1305 AEAD、Ed25519 签名、HKDF 密钥派生
- **安全不变量**：19 项（INV-01 ~ INV-19），覆盖内存清零、篡改拒绝、错误密码拒绝、多 Profile 锁定零化等
- **金库生命周期**：创建 / 解锁 / 锁定 / 修改密码 / 保存 / 加载
- **多 Profile**：DEK 隔离、同步参与、自动锁定、切换
- **同步**：向量时钟合并、冲突解决、WebDAV 集成、重放攻击防御
- **搜索**：全文搜索、字段级语法（`tags:` / `platform:` / `field:` / `expired`）
- **TOTP**：RFC 6238 附录 B 6 个标准测试向量
- **导入导出**：Bitwarden / KeePass XML / KDBX / 1Password / EnPass / CSV / .env / Seed
- **平台轮换**：OpenAI / GitHub API 密钥自动轮换
- **更新服务**：安装包识别、下载进度、版本比较
- **浏览器扩展**：API 搜索、Profile 选择、端口配置
- **Roslyn 分析器**：OKV0001（禁止 string 密码参数）、OKV0003（禁止 == 比较密钥）

---

## 2. 性能基准

### 1 万条目压测（`tools/OmniKeyVault.Benchmark`）

| 场景 | 实际 | 目标 | 状态 |
|---|---|---|---|
| 创建 1 万条目 Vault | 0.7 s | ≤ 60 s | ✅ |
| 解锁（Argon2id 64 MiB） | 0.1 s | ≤ 1.5 s | ✅ |
| 全文搜索 1 万条目（P50） | 1.5 ms | ≤ 200 ms | ✅ |
| 2 实例同步 1 万条目 | 0.0 s | ≤ 5 s | ✅ |

> Benchmark 使用 `OKV_TEST_MODE=1` 切换到 64 MiB Argon2id（生产 256 MiB）。运行方式：`dotnet run --project tools/OmniKeyVault.Benchmark`

---

## 3. 密码学不变量

| ID | 不变量 | 状态 |
|---|---|---|
| INV-01 | `ICryptoProvider` 不接受 `string` | ✅ 编译期（Roslyn OKV0001） |
| INV-02 | 唯一 24B nonce | ✅ |
| INV-03 | `SecureKey.Dispose` 清零内存 | ✅ |
| INV-04 | 锁定后 Service 调用抛异常 | ✅ 全服务覆盖 |
| INV-05 | Ed25519 签名覆盖 | ✅ |
| INV-06 | Argon2id `m ≥ 256 MiB` | ✅ |
| INV-07 | `FixedTimeEquals` | ✅（Roslyn OKV0003） |
| INV-08 | AEAD 失败不返回部分明文 | ✅ |
| INV-09 | 同步路径仅传输密文 | ✅ |
| INV-10 | 崩溃转储不含 MK | ⏸ 平台特性 |
| INV-11 ~ INV-19 | 扩展安全不变量 | ✅ |

---

## 4. 威胁防御对照

| 威胁 | 防御措施 | 测试覆盖 |
|---|---|---|
| 云盘被入侵 | 零知识加密，服务器只见密文 | ✅ |
| 离线爆破 | 双重 Argon2id（320 MiB）+ 自定义格式 | ✅ |
| 源码+金库同时泄露 | 双轮 KDF + 域分隔符 | ✅ |
| 内存 dump | 敏感数据用完即清零 | ✅ |
| 篡改文件 | AEAD 认证 + Ed25519 签名 | ✅ |
| 重放攻击 | 向量时钟比较 | ✅ SEC-T7-01 |
| 备份介质丢失 | Seed 强制 dev/test Profile + prod 拒绝 | ✅ |

---

## 5. 复现指南

```powershell
# 在仓库根目录
dotnet restore OmniKeyVault.sln
dotnet build OmniKeyVault.sln -c Release

# 运行所有 791 个测试
dotnet test

# 运行性能压测
dotnet run --project tools/OmniKeyVault.Benchmark -c Release
```

**预期输出**：
- 测试：791/791 通过，0 失败，0 跳过
- 压测：4/4 场景全部达标

---

## 6. 界面功能验证

v2.3.8 界面全功能验证：142/142 项通过（100%），覆盖：
- 金库创建 / 解锁 / 锁定
- 条目 CRUD / 搜索 / 排序
- Profile 切换 / 设置
- 导入导出（7 种格式）
- 同步（WebDAV / S3 / 本地）
- 工具区所有功能按钮
- 快捷键体系
- 浏览器扩展
- 全局热键
- 命令面板 / 全局搜索
- 通知中心 / 安全健康报告
- 辅助功能设置

---

**报告结束**
